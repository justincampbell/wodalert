{{#each flashMessages.queue as |flash|}}
  {{#flash-message flash=flash as |component flash close|}}
    <div class="notification is-{{lowercase component.flashType}}">
      <button {{action close}} class="delete"></button>
      {{flash.message}}
    </div>
  {{/flash-message}}
{{/each}}
